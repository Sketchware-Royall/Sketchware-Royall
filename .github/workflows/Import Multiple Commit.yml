name: Import Single PR/Commit → One Branch

on:
  workflow_dispatch:
    inputs:
      url:
        description: "PR or Commit URL"
        required: true

jobs:
  import:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

      - name: Checkout base branch
        run: |
          BASE="${{ github.event.repository.default_branch }}"
          git checkout $BASE
          git pull origin $BASE

      - name: Create new branch
        run: |
          BRANCH="import-$(date +%s)"
          echo "BRANCH=$BRANCH" >> $GITHUB_ENV
          git checkout -b $BRANCH

      - name: Apply URL
        run: |
          URL="${{ github.event.inputs.url }}"
          echo "Processing: $URL"

          OWNER=$(echo $URL | cut -d'/' -f4)
          REPO=$(echo $URL | cut -d'/' -f5)

          git remote remove external 2>/dev/null || true
          git remote add external https://github.com/$OWNER/$REPO.git
          git fetch external

          if [[ "$URL" == *"/commit/"* ]]; then
            ID=$(echo $URL | cut -d'/' -f7)
            git cherry-pick $ID

          elif [[ "$URL" == *"/pull/"* ]]; then
            ID=$(echo $URL | cut -d'/' -f7)
            git fetch external pull/$ID/head:external-pr
            git merge external-pr --allow-unrelated-histories
          fi

      - name: Push branch
        run: |
          git push origin $BRANCH